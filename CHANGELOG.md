# 版本记录

## 0.2.3

- 按已确认的 EcoPaste 风格重做原生界面：紧凑标题与搜索栏、图标分类、独立记录卡片和统一编辑页。
- 原置顶记录改用星标收藏，可从列表直接切换收藏、筛选收藏；已有数据保持兼容。
- 背景图片透明度独立于卡片、标题栏和按钮，完整显示背景时仍保留清晰实色控件。
- 新安装默认采用 424 × 634 小窗和蓝白配色；旧窗口位置、尺寸、自定义配色及图片继续保留。旧版默认配色自动升级。
- 保留原生拖动、快捷键按键录入和加密密码库；增加 Ctrl+F 搜索，锁定密码库时清空账号搜索内容。

## 0.2.2

- 标题区域可直接拖动窗口，重新打开时记住上次位置与大小；显示器布局变化时把窗口移回可见区域。
- 调整备忘录、密码库与设置的按钮和卡片层级，选择自定义背景图片后仍保留可读的控件底色。
- 使用完全不透明的高对比背景图检查显示，并补充 Windows 原生命中测试与窗口位置回归测试。

## 0.2.1

- 快捷键设置改为点输入框后直接按键录入，支持功能键和常见组合键。
- 被占用的按键显示错误，取消设置后恢复原快捷键。
- 阻止 Alt+F4 系统关闭快捷键被绑定，并补充真实窗口交互测试。

## 0.2.0

- 改为 Windows 原生轻量小工具，运行文件约 96 KB，双击即用，无需安装。
- 去掉 Electron、内置浏览器和 npm 运行依赖，使用系统 .NET Framework 4.8。
- 默认快捷键改为 F8，保留可配置快捷键、托盘、置顶和单实例。
- 配置、背景图片、普通记录和加密密码库移至运行文件旁的 data 文件夹。
- 保留备忘录编辑搜索、加密账号、密码生成复制、自动锁定、主题/图片透明度与备份恢复。
- 密码库使用新的 v2 格式；旧版资料原位保留，旧加密备份不直接导入。
- 发布内容改为单文件和轻量 zip；更新时只需替换运行文件并保留 data。

## 0.1.0

首个 Electron 桌面版本，包含安装器与免安装包。源码与发布文件保留在 v0.1.0 标签和 Release。
